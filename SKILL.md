---
name: travel-itinerary
description: 英国／欧洲定制旅行行程制作。当用户说"做行程"、"排框架"、"逐日行程"、"这单"、"客户要去XX"、"出文档1/文档2"或 /travel-itinerary 时使用。从客户需求排框架、按编号路线搬模板做逐日详细行程、出预约汇总表；客户沟通由用户负责，AI 不介入。
---

# 旅行行程制作

本目录就是知识库本体（同时是 git 工作副本，可直接 `git pull` / `git push`）。
**规则只在 `docs/铁律.md`，本页不复述**，只写做什么、按什么顺序、用哪个工具。

## 开工前读三份

| 读什么 | 管什么 |
|---|---|
| `docs/铁律.md` | 规则：时间、变量、内容、交付口吻、格式、核对、优先级 |
| `docs/制作指南.md` | 做法：排框架 → 做逐日 → 出稿 → 交付 |
| `docs/质检清单.md` | 检查：每天做完、交付之前各过一遍 |

做欧洲订单再加 `docs/欧洲通用避坑规则.md`（跨国交通站名、票务打卡、退税盖戳、周日关门）。

## 四步

**① 建客户变量表** — 用 `docs/客户变量表模板.md`。客户沟通是用户的活，AI 只整理用户给的信息，不自行补关键决定。

**② 排框架** — 先列闭馆矩阵（把每日开放的项目放在闭馆最多那天），再选模板：

```bash
python3 tools/取路线.py --list 罗马     # 列出该城所有路线＋图片数
```

**选图片最多的那版**（图多＝最近做的新模板）。客户在一城住几天，就用模板里对应的「几天」套装，对照表在 `docs/制作指南.md`。

给客户的框架用 `templates/框架-给客户看.md` 的表格版或分段版，**不带路线编号和匹配度**——那是给用户的内部标注，只在对话里说。

**③ 逐日详细行程** — 客户确认框架后，一天一天做，不要一次生成全部。

```bash
python3 tools/取路线.py ROM-R02          # 只取这一张表，不通读整份 DOCX
python3 tools/拼行程.py 配方.json 输出.docx  # 按编号搬表＋行级改写＋自检
```

**必须搬表，不能重打内容**——重打会丢光模板里的图片和格式（实测 42 张图变 0 张）。配方字段见 `tools/README.md`，完整示例 `tools/配方示例.json`。

每天从起点写起（落地日写落地进城、转场日写退房去车站、平常日写从酒店出发），写到回酒店，中间每段移动都写清怎么走。

**④ 出稿与交付** — 两份文件分开，不合并成一个 PDF。

```bash
python3 tools/比对.py 我生成的.docx 用户改过的.docx   # 用户改过稿后必跑
python3 tools/看成品.py --list                        # 交付前看一份同类成品
```

- 文档1（每日详细行程）：骨架 `templates/文档1-每日详细行程.md`
- 文档2（预约与汇总表）：骨架 `templates/文档2-预约与汇总表.md`，链接要可点击、官网与国内平台各一列、核心提醒只写一句话

## 工具

| 工具 | 什么时候用 |
|---|---|
| `tools/取路线.py` | 按编号取单张表；`--list 关键词` 反查编号并显示图片数 |
| `tools/拼行程.py` | 做逐日行程。搬表＋行级改写＋自检（时间倒流／时段重复／旧客户信息／内部用语） |
| `tools/比对.py` | 用户改过稿后，做文档2 前先跑，逐行列出改动 |
| `tools/看成品.py` | 交付前看已交付成品的实际颗粒度 |
| `tools/打标.py` | 只在新增模板时用，给 DOCX 打路线编号标签 |

模板库路径在 `config/local-paths.yaml`（gitignore，需本机配置）；未配置时工具默认找 `~/文件夹/旅游通用模板`。

## 索引

- `catalog/英国城市模板索引.md`、`catalog/欧洲城市模板索引.md` — 城市页跳转表
- `catalog/路线定位表.csv` — 174 条路线：编号 → 模板文件 → 第几张表 → 图片数
- `cities/<国家>/<城市>.md` — 每城的编号路线、调序规则、已知问题、「改这天要动哪几行」

## 几条边界

- 客户沟通（需求收集、框架确认）由用户做，仓库不存沟通话术
- 交付文件面向客户，不得出现"客户/模板/沿用/新增内容"等内部用语，`拼行程.py` 会自动查
- 判断文件是模板还是成品：看内容有没有客户特定信息（日期、人数、航班号、酒店名），有就是成品，不能当模板用
- 本目录的改动记得 `git push`，它同时是 GitHub 仓库的工作副本
